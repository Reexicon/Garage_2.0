@model IEnumerable<Garage_2._0.Models.Member>

@{
    ViewBag.Title = "Members";
}

<h2>@ViewBag.Title</h2>

<p>
    <span class="buttonspan">
        @Html.ActionLink("Register member", "Create")
    </span>
</p>


@using (Html.BeginForm("Index", "Members", FormMethod.Get))
{
    <table class="table tblShadow">
        <tr>
            <th>
                First Name:
            </th>
            <th>
                Last Name:
            </th>
            <th>
            </th>
        </tr>
        <tr>
           <td>
                @Html.TextBox("SearchFirstName")
            </td>
            <td>
                @Html.TextBox("SearchLastName")
            </td>
            <td>
                <input type="submit" value="Search" />
            </td>
        </tr>
    </table>

}
<table class="table tblShadow">
    <tr>
        <th>
            @Html.ActionLink("First Name", "Index", new { sortOrder = ViewBag.FirstNameSortParm, searchFirstName = ViewBag.SearchFirstName, searchLastName = ViewBag.SearchLastName })
        </th>
        <th>
            @Html.ActionLink("Last Name", "Index", new { sortOrder = ViewBag.LastNameSortParm, searchFirstName = ViewBag.SearchFirstName, searchLastName = ViewBag.SearchLastName })
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }


    </table>
